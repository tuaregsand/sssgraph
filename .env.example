# Root environment template for this monorepo.
# Runtime env files are service-specific:
#   cp api/.env.example api/.env
#   cp ingester/.env.example ingester/.env
#
# Optional laserctl hub variables:
# Absolute path to a local override file consumed by `./laserctl hub`.
LASERCTL_CONFIG=$HOME/.laserctl.env
# Optional event and process logs for the hub dashboard.
LASERCTL_LOG_FILE=/tmp/laserctl-events.log
LASERCTL_API_LOG_FILE=
LASERCTL_INGESTER_LOG_FILE=
